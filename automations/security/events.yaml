# Security Events


- id: security_device_triggered_away
  alias: Raise Security Away Events
  trigger:
    - platform: state
      entity_id: !include ../../security/away.yaml
      to: 'on'
  action: 
    - event: security_device_triggered
      event_data_template:
        entity_id: "{{ trigger.entity_id }}"
        away: true